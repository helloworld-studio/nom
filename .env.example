# Solana RPC URL (required)
# Replace with your own RPC endpoint
RPC_URL=your_rpc_url_here

# LLM API Key (required for AI analysis)
LLM_API_KEY=your_llm_api_key_here

# CORS Origin (required in production)
# For production, specify comma-separated list of allowed domains
CORS_ORIGIN=https://nom-ibs6.onrender.com

# Development CORS Origin (optional)
# For local development, specify comma-separated list of allowed domains
# Defaults to http://localhost:3000 if not specified
DEV_CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# Port (optional)
# Defaults to 5000 if not specified
# For Render.com deployment, use 10000
PORT=10000

# Node environment (required for production)
# Set to 'production' for deployment
NODE_ENV=production
