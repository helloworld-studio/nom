# Solana RPC URL (required)
# Replace with your own RPC endpoint
REACT_APP_RPC_URL=your_rpc_url_here

# Backend API URL (required)
# In development, defaults to http://localhost:5000 via proxy
# In production, set to your deployed backend URL
REACT_APP_API_URL=http://localhost:5000
