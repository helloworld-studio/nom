
# Backend API URL (required)
# In development, defaults to http://localhost:5000 via proxy
# In production, set to your deployed backend URL (e.g., https://nom.onrender.com)
REACT_APP_API_URL=http://localhost:5000

# For Render.com deployment, these variables need to be set in the Render dashboard
# and will be automatically included during the build process
